POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=transfer_schedule

JWT_SECRET=dev_local_super_secret_123456789
JWT_EXPIRES_IN=7d

# Public URL where frontend is available (used by backend CORS)
FRONTEND_URL=http://localhost

# Optional
APP_PORT=80

# Optional: override DB URL when using managed PostgreSQL
# DATABASE_URL=postgresql://user:password@host:5432/dbname
